# UiGridContainer

头文件：`engine/ui/containers/ui_grid_container.h`。网格焦点容器。

调用：`add_child` 返回已接管 child；`set_column_count`、`set_cell_spacing`、`set_fill_by_row` 配置网格。`content_extent` 返回 explicit/intrinsic 中较大值。
