# UiNumber

头文件：`engine/ui/widgets/number/ui_number.h`。共享数字纹理缓存的数值元素。

调用：`set_value`；`set_decimal_places`、`set_trim_trailing_zeros`、`set_keep_decimal_point`、`set_suffix` 控制格式；`set_digit_spacing`、`set_fixed_glyph_advance`、`set_target_height` 控制字形几何；支持对齐、typography 与样式 API。
