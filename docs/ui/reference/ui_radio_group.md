# UiRadioGroup

头文件：`engine/ui/containers/ui_radio_group.h`。维护 `UiRadioItem` 的互斥选择。

调用：将 `UiRadioButton` 或 `UiLabeledRadioButton` 作为 child 添加；`selected_index`、`set_selected_index`、`set_on_selection_changed` 与 ButtonGroup 语义相同。
