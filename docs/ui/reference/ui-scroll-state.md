# UiScrollState

头文件：`engine/ui/scroll/ui_scroll_state.h`。纯滚动状态对象。调用 `set_axis`、`set_viewport_size`、`set_content_size`、`set_offset`、`set_step`；用 `offset`、`max_offset`、ratio 与 `scroll_by`/`scroll_to_*` 操作或查询。`UiScrollContainer` 通常封装它。
