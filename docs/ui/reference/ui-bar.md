# UiBar

头文件：`engine/ui/widgets/ui_bar.h`。非交互数值填充条。

调用：`set_range` + `set_value` 使用数值范围，或 `set_ratio` 直接设归一化值；`set_fill_direction` 选择四个 `BarFillDirection`；`ratio`、`value`、`min_value`、`max_value` 查询结果。
