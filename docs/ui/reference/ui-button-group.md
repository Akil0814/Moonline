# UiButtonGroup

头文件：`engine/ui/containers/ui_button_group.h`。互斥选择的 Button List。

调用：`add_button` 接管 `UiButton`；`selected_index` 查询；`set_selected_index` 选择；`set_auto_select_first` 设置空选择策略；`set_on_selection_changed` 接收 `optional<size_t>`。
