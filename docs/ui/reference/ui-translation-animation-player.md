# UiTranslationAnimationPlayer

头文件：`engine/ui/effects/ui_translation_animation_player.h`。命名 translation animation 状态机。调用 `bind(name, animation)`、`play(name)`、`stop`、`update(delta)`；`translation` 和 `is_playing` 查询结果。`UiElement` 提供对应的高层转发 API。
